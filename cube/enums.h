#pragma once

enum class Color
{
    RED,
    BLUE,
    ORANGE,
    WHITE,
    GREEN,
    YELLOW,
    ERROR
};

enum class Face
{
    UP,
    DOWN,
    LEFT,
    RIGHT,
    FRONT,
    BACK
};

enum class Direction
{
    CLOCKWISE,
    ANTI_CLOCKWISE
};

enum class Mode
{
    SIMULATION,
    ROBOT
};
